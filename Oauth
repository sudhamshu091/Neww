So, user tries to login, application redirects to authorization server page. Where he enters the credentials and the credentials are verified by Authorization server and a Authorization code or grant is provided. So at the client side, it send s the Authorization code along with the client id and secret and Authorization server validates it. After that it allocates a Authorization token to the client side which the user can use to access the protected resources.
